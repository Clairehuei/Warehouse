<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:divider="@drawable/spacer_24dp"
    android:orientation="vertical"
    android:showDividers="middle">

    <include layout="@layout/toolbar_actionbar" />

    <android.support.v7.widget.GridLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        app:alignmentMode="alignBounds"
        app:columnCount="3"
        app:columnOrderPreserved="true"
        app:orientation="horizontal"
        app:rowCount="3">

        <Button
            android:id="@+id/classify"
            style="@style/MainButton"
            android:drawableTop="@drawable/btn_category_search_selector"
            android:text="@string/classify"
            app:layout_gravity="center" />

        <Space />

        <Button
            android:id="@+id/sing_out"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:drawableTop="@drawable/ic_logout_selector"
            android:text="@string/sign_out"
            android:textColor="@color/logout_button_text_color_selector"
            app:layout_gravity="center|bottom" />


    </android.support.v7.widget.GridLayout>

</LinearLayout>